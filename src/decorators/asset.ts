// import {createDecorator} from 'vue-class-component';
//
// export default createDecorator(async options => {
//   const basedir = `components/${(options as any).name.toLowerCase()}`;
//   console.log(basedir);
//   options.template = await import(`${basedir}/template.vue`);
//   await import(`${basedir}/style.less`);
//   return options;
// });
